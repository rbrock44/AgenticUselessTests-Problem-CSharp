# Testing Rules

- Do not write getter roundtrip tests that only verify a getter returns what was passed to the constructor.
- Do not write store/retrieve container tests that only verify an item can be added to a collection and read back.
- Test observable behavior, not implementation details.
